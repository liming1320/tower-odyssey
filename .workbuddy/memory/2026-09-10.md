# 2026-09-10 · 模拟器 ROM 改服务端架构（管理员上传 · 全员游玩）

## 1. 需求
玩家反馈：ROM 导入应只有管理员能做；文件存服务器（不是浏览器 IndexedDB）；所有玩家共享管理员上传的游戏库。

## 2. 服务端（server.js）
- **存储方案**：ROM 二进制存 `data/roms/<id>.bin` 磁盘文件（gitignore 已加），元数据 `DB.roms[]` 走 save()（db.json / MySQL 模式均兼容）——二进制大文件不塞 JSON/MySQL，磁盘+元数据是通行做法
- **路由**：
  - `GET /api/roms`（登录）→ `{roms:[元数据], admin:bool}`（admin 字段告知前端是否显示导入区）
  - `GET /api/roms/download?id=`（登录）→ 流式回传文件
  - `POST /api/roms/upload?name=&core=`（管理员）→ 原始二进制流式落盘，512MB 上限，带背压
  - `POST /api/roms/delete`（管理员）→ 元数据+磁盘文件一起清
- **管理员判定**：`romAdminOk` 双通道——后台独立令牌（admin/workbuddy 的 `__admin__`）或玩家端 isAdmin 账号（第一个注册的玩家）
- **关键坑**：路由分发对所有 POST 先 `readBody`（JSON 字符串解析毁二进制）→ 上传路径在分发处特判跳过；上传 handler 首版漏了 `out.write(c)`（只计数没写盘，下载 0 字节），修复+背压

## 3. 客户端（emulator.js v=20260910a 重写）
- 删掉 IndexedDB，全部走服务端 API（game-token Bearer）
- 导入区/核心选择器/删除键仅 `admin:true` 时渲染；玩家只有列表+播放
- 播放：`/api/roms/download`（鉴权）→ blob → objectURL → EmulatorJS iframe（不变）
- 空态分角色文案（管理员"上方导入后全员可见" / 玩家"管理员还没有上传游戏"）

## 4. 验证
- API 层 8 步全过：admin 登录→上传→玩家列表(1条,admin:false)→下载(逐字节一致)→玩家上传被拒403→匿名下载401→删除→列表清零
- 浏览器冒烟 `tools/smoke-emulator.js` **17/17**：玩家看到 ROM+「admin 上传」、无导入区无删除键、播放 iframe blob、引擎真实加载 canvas 创建、管理员删除后玩家列表即时清空
- 全量审计 108 款 0 异常 0 问题

## 5. 服务重启注意
- `node server.js` 在 Git Bash 里 `(cmd &)` 后台方式会随 shell 退出被回收 → 必须用工具的后台任务方式启动（当前 task zbbhSv 在跑）
- wmic 被安全策略黑名单挡了，查进程用 `netstat -ano | grep :5180`

## 15. 模拟器 ROM：挪到后台 /admin 管理（按用户要求）

**用户要求**：导入区必须放在 `/admin` 管理后台，玩家端只浏览不导入。ROM 不存网页存服务端。

**改造**：
- `public/admin.html`：标签栏追加 `模拟器ROM` 标签（`<button data-t="roms">`）
- `public/js/admin-app.js`：注册 `AdminAPI.romList / romDelete` → 标签分发 `roms: () => this.renderRoms(body)` → 实现 `renderRoms / romRefreshList / romUpload / renderCorePicker / onDrop`（拖拽 + 点选 + 9 核心 picker + 列表删除 + 鉴权确认）
- `public/js/minigames/emulator.js`：玩家端彻底精简——**删掉导入区、核心选择器、删除键、拖放**，只剩「拉列表 + ▶播放」。删除按钮 selector `.emu-btn-del`、导入区 selector `.emu-drop` 在 DOM 中都不渲染

**冒烟（端到端真实浏览器）18/18 全过**：
- ① 账号 · ② 后台标签 + 拖入区 + 列表 · ③ .nes 自动识别 FC · ④ .zip 核心选择器 → DOS
- ⑤ 玩家列表看到本轮 2 个 · **无导入区 · 无删除按钮 · 1 个播放按钮**
- ⑥ 玩家播放 → iframe + EJS_core=nes + 鉴权 blob · **EmulatorJS 真实加载（canvas 已创建）**
- ⑦ 后台删除 → 玩家同步清空

冒烟脚本用 `emu-smoke-` TAG 隔离本轮痕迹，启动 + 收尾双重清理，不会污染玩家数据。

**审计**：108 款小游戏 0 异常

**待用户推送**：git push origin master（在 adfa050 之后）
