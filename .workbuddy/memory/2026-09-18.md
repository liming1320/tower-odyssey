# 2026-09-18 工作日志

## server.js 模块化拆分（增量拆 + verify-routes 闸门）

- **安全网**：server.js 加 `MG_NO_LISTEN`（不绑端口）+ `MG_DATA_DIR`（临时数据目录）+ 末尾 `module.exports={api,DB}`；
  `tools/verify-routes.js`（`MG_NO_LISTEN=1` 加载 server.js，断言 111 路由齐全 + 实跑几个 GET handler）。
- **deploy.sh 自愈**：webhook 自动部署原脚本不跑 npm install（靠 ws 侥幸在位），已加「仅 package.json 变动才 npm install」+ ws 缺失判失败回滚。结论：纯代码改动只需 push，无需手动 npm install。
- **已抽 camp.js**：营地/建筑/城墙/古宝 6 handler（routeCtx 注入 productionRates/gainOf/applyProduction/buildingCost/canAfford/missingRes）。
- **已抽 heroes.js**：GET /api/heroes + 15 个 POST /api/hero/*；routeCtx 注入 STAR_BASE/STAR_MAX/STAR_PERKS/WISH_PITY/RES_CN/U_NUM/FORGE_MAX_LV/QUALITIES/QUALITY_NAME +
  starUpCost/starUpMaterialCost/forgeLevelCost/forgeQualityCost/equipStats/heroCombatStats；4 个域内私有 helper（materialHeroUids/findOwnHero/equipInstOf/payCost）搬进 heroes.js。
  踩坑：首版漏了上阵/下阵 `POST /api/hero/equip`、`POST /api/hero/unequip`（队伍最多 5 人），verify 直接报缺失 → 补回后 6/0 通过。
- **关键经验**：抽路由时 handler 调用的顶层 helper（函数声明会被 hoist，跨域共享的留在 server.js 经 ctx 注入；仅本域用的搬进模块）；纯数据常量已全在 `server/config` barrel，server.js 顶部解构，故 heroes.js 也经 ctx 注入。

## 下一步
tower(+world) → events+wish → clan+chat+mail → minigame+pk32+gift → admin* → server/config 扩装备/戒指/宝石/技能/锻造/活动模板。每抽一个域跑一次 verify-routes.js。

## 本轮续做（同 session 下午）
- **已抽 clan.js**：clans/clan.create/join/mine + user/set-nickname + chat(GET/send) + mail(GET/claim)；
  displayName/validNickname/newDisplayId 为 server.js 共享 helper 经 ctx 注入。verify 6/0。
- **已抽 minigame.js**：minigame 注册表（MINIGAMES_FILE + MINIGAME_BASE_IDS + minigameRegistry + mgIds/mgSet/mgNames +
  MINIGAME_IDS/MINIGAME_NAMES 兼容代理）+ report/progress/score/rank/order + pk32 注册表(PK32_FILE+pk32Registry) + pk32/order
  + admin/minigame/order + admin/pk32/order。ROM 常量块(ROMS_DIR 等 1694–1723)留 server.js（已被 rom.js require）。
  路径注意：minigame.js 在 server/routes/，故 MINIGAMES_FILE/PK32_FILE 用 `path.join(__dirname,'..','..','public',...)` 回指项目根。
- **已抽 gift.js**：gift/redeem（admin/gift/save+delete+list 留 admin）。
- server.js 现 ~2123 行，7 个 require('./server/routes/*')。5 个玩家域(camp/heroes/tower/events/clan)+minigame+gift 全部抽完，verify 全 6/0、路由表 111 零缺失。
- **关键坑（已规避，记入 MEMORY）**：verify-routes 闸门只实跑 health/camp/heroes/tower/info，**未覆盖 GET /api/tower/level
  （调 chapterOf 等闭包）与各 admin 路由（需 admin token）**。抽 admin 前必须先加固闸门（临时 db 造测试玩家+admin token，
  实跑 tower/level、admin/overview 等），否则闭包缺变量类回归上线才爆。

## 待办（下一步）
1. 加固 tools/verify-routes.js：临时 db 造 tester 玩家 + admin token，实跑 GET /api/tower/level、GET /api/admin/overview 等。
2. 抽 admin*（server/routes/admin.js）+ account/delete + user/bind-phone+set-password，共享 helper（verifyPassword/Store/tavern*/normalizeSkills 等）经 ctx 注入。
3. server/config 扩：装备/戒指/宝石/技能/锻造/活动模板纯数据抽到对应 config 文件，index.js barrel 聚合。
