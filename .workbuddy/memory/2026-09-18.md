# 2026-09-18 工作日志

## 范围澄清（用户指令）
- **pk32 系列全部排除**（18 个 `pk32*.js` 文件不改动）。
- 目标 = 小游戏中 ~123 款（即全部 MiniGames 减去 `pinball.js` 三维弹球，再减 pk32）。
- `optimized/` 的 `zumaplus/bubbleplus/rocketmaniaplus/mummymazeplus-rules` **未加载**（index.html 无引用）→ 属死代码，按"123 款"口径排除，不纳入。
- **禁止修改 `pinball.js`（三维弹球）**（绝对硬约束，一直有效）。

## 音效（SFX）统一升级 Batch9 —— 用"本地 E 副本包装"手法一次性覆盖合集
- 9 个 `E.def/E.defd` 合集文件：在本地 `const E = Object.assign({}, MG.eng)` 副本上包装 `E.def/E.defd`，给该文件每个游戏的 `tap` 自动加 `MG.audio.unlock()+sfx('click')`。作用域仅限本文件，不动全局引擎，不与已做 16 款重复。
  - 文件：`mg-board`(12款)、`mg-casual`(12)、`mg-action`(12)、`mg-puzzle`(13)、`mg-sim`(13)、`mg-card`(8)、`mg-cooking`(1)、`mg-quiz`(13,含defd)、`mg-memory`(12,含defd) → 约 108 款。
- `mg-danmaku.js`（自绘 canvas，走 E.game 已自动 unlock）：仅加离散音——`doBomb`→`launch`、`playerHit`→`fail`（自动击杀太频繁不发音）。
- `mg-tower-core.js`（tower24/50/newtower56 共用，走 E.game）：在两处 `api.haptics.tap()` 前加 `sfx('click')`（移动/开门/拾取反馈）。
- `?v` 全部升 `20260918a`（用 node 原子替换，规避同文件多次 Edit 的 EBUSY 回滚）。
- 累计：此前 39 款 + 本批 ~108 + danmaku(1) + 魔塔(3) ≈ 全量覆盖（除 pinball/pk32/死代码）。

## EBUSY 坑（持续）
- 同文件一条消息发多个 Edit 会并发回滚（richman/monopoly/mg-sheep/tower-core 均中招）。
- 解法：同文件改动要么单条发+grep 复核，要么用 node 脚本整体原子替换（如本次 ?v 批量更新）。
- 跨文件一条消息多个 Edit（每文件仅 1 处）安全。

## smoke 验证
- `diDukt`：tictactoe/coinflip/flappy/maze/towerdef/solitaire/mathquiz/chimp/danmaku/tower50 → **10/10 通过，零 JS 报错、无空白渲染**（`tools/shots/_smoke_batch9.txt`）。

## 引擎优化（用户要求 P0+P1+P2 全做，2026-09-18 下午）
改 4 个引擎文件：`mg-render.js`/`_engine.js`/`mg-audio.js`/`mg-effects.js`，`?v` 升 `20260918b`，smoke `ZIpWgi` → **7/7 通过零报错**（`tools/shots/_smoke_opt.txt`，含 reversi/connect4/gomoku 验证 broom/piece 离屏缓存）。
- **P0-1 画质缓存按类型分桶**：`MG.gfx._cache` 单 Map → `_caches{px,scene,wood,glow}` 各自独立上限，消除 px 精灵把 scene/wood 挤掉引发的每帧重建抖动。
- **P0-2 E.broom/E.piece 离屏缓存**：按 (尺寸+材质+seed+画质) / (c1/c2/rim/r+画质) 烘焙到离屏 canvas，之后每帧只 drawImage；棋牌类每帧不再建多个径向渐变+四角铜钉。
- **P1-3 关卡懒生成**：`E.def/E.defd` 的 `LEVELS` 改为 getter，首次读取（用户点开选关页）才 `buildLevels`，首屏零关卡成本、行为一致（desc/params 仍在）。
- **P1-4 拖拽 rect 缓存**：`onDown` 缓存 `getBoundingClientRect`，`onMove/onUp` 复用，避免拖拽期每事件触发 reflow。
- **P1-5 SFX 去抖/静音**：`sfx` 加同音 20ms 去重 + muted 早返回，防爆音/省合成。
- **P2-6 autoQuality 生效**：`render()` 把 `quality` 注入 `fx._quality`，粒子池 quality<1 时按比例减粒子。
- **P2-7 BGM 随游戏 stop 自动停**：`E.game.stop`/`E.dgame.stop` 调 `MG.audio.bgm.stop()`，防 setTimeout 调度器泄漏。
- **P2-8 resize rAF 节流**：`MG.canvas` 的 resize 监听改为 rAF 节流，`destroy` 同步移除。
- **P2-9 text 字体串缓存**：`G.text` 缓存 `ctx.font` 拼接串，省重复构建。
- 关键坑：panel 主体渐变**未**缓存（createLinearGradient 锚定绝对 y，跨 y 复用会错位）；broom/piece 离屏按画质倍率烘焙并 drawImage 回逻辑尺寸（1:1 像素）。

## 引擎功能增强（用户要求 Tier1+Tier2+Tier3 全做，本会话收尾）
新增 10 个引擎模块（`public/js/minigames/engine/mg-*.js`）+ 改 4 个引擎文件，`?v` 升 `20260918c`，smoke（gomoku/g2048/tictactoe/connect4/reversi/danmaku/tower50/shooter/snake/match3/sudoku6/breakout）→ **12/12 通过，零 JS 报错、无空白渲染**（`tools/shots/_smoke_tier.txt`）。
- **Tier1-1 撤销栈** `MG.history`（挂在 `E.game`/`E.dgame` 的 `api.history`，`push/undo/canUndo/clear/size`；优先 `cfg.snapshot/restore`，否则 `structuredClone`/JSON 兜底）。
- **Tier1-2 设置+无障碍** `MG.settings`（音量0.42/震动/自动画质，localStorage 持久化，boot 自动 `load()`；包裹 `MG.haptics` 受开关控制）+ `MG.a11y`（reducedMotion/colorblind/largeText）；`runLevel` 每关挂齿轮面板 `MG.settings.gear(container)`；`mg-effects` 的 `burst/cam.shake` 读 reducedMotion。
- **Tier1-3 确定性重放（降维）** `mg-replay.js`：`MG.ri` 是 `Math.random` 不可重播种 → 真·确定性重放不可行，改为**输入录制/回放**（`api._rec.events=[{t,x,y,k}]`，`_engine.doTick` 按时间戳重注入 tap/key）。已在文件头注明限制。
- **Tier2-4 新人引导** `MG.coach(parent,steps)`：径向遮罩高亮 + 提示气泡 + 上/下步（steps=`[{get:()=>el|rect,text,arrow?}]`）。
- **Tier2-5 成就里程碑** `MG.achievements`：`define(id,d)/has/unlock`，localStorage + POST `/api/minigame/achievement` + toast。
- **Tier2-6 手势增强** `_engine` 输入层重写：`_ptrs` Map + `_dispatchTap/_dispatchKey`，支持 `cfg.drag/doubletap/longpress(500ms)/pinch(2指)/multitouch` 与 doubletap 聚类。
- **Tier3** `MG.snapshot`（断点续玩 save/load/clear）、`MG.telemetry`（`track` 队列20→POST `/api/minigame/telemetry`，`beforeunload` flush；`runGame.onComplete` 调用）、`MG.makeSprite`（帧动画时间轴 `update/draw/play/pause/reset`）、`MG.combo`（连击衰减+分档倍率）、`MG.i18n`（`set(dict,lang)/t(s)` 文本表）、`MG.devHud(container,getPerf)`（rAF 性能浮层）。
- `mg-audio`：`setVolume`/`bgmGain` GainNode，`tone/noise` 收 `o.dest`，`bgm._note` 走 `bgmGain`，`sfx` 触发 BGM 短暂 ducking（0.35→1）。
- `main.css` 追加 `.mg-gear/.mg-coach/.mg-devhud` 样式；`index.html` 在 `_engine.js` 后注入 10 个模块 `<script>`。
- 关键坑：`_patch_css_index.js` 的注入锚点用 8 空格，而 index.html 那行是 tab 缩进 → 首次只升了 `?v` 没注入模块；用 `_patch_css_index2.js`（正则 whitespace-insensitive）补注入。全程用 node 原子补丁脚本（锚点 `indexOf` 校验）规避 EBUSY。
- **硬约束守恒**：`pinball.js`、`pk32*`(18) 未改动；`arcade.js`/`emulator.js` 为包装层跳过；`optimized/` 死代码跳过。

## 引擎功能增强（A 画质 / B 玩法 / C 健壮性，本会话收尾——用户"A B C 都做"）
新增 8 个引擎模块（`public/js/minigames/engine/mg-postfx/mg-bg/mg-skin/mg-daily/mg-assist/mg-scorefx/mg-localmp/mg-pwa.js`）+ `public/sw.js`（Service Worker），改 6 个引擎文件（`mg-render`/`mg-effects`/`mg-i18n`/`mg-ui`/`_engine`/`mg-settings`，其中 `mg-settings` 整体重写），`?v` 升 `20260918d`，smoke 全量 **124/124 通过，零 JS 报错、无空白渲染**（`tools/shots/_smoke_abc.txt`）。
- **A1 渲染倍率/像素风**：`mg-render` 加 `_rs`(renderScale 0.75~1.5) 与 `pixelated` 硬边放大 toggle（DPR HiDPI 此前已有，本项只补上述两项）。
- **A2 后处理** `MG.postfx`（off/soft/retro）：paint 末尾合成层，bloom 近似(downscale+lighter，高画质才做)+暗角+复古扫描线；`_engine` 注入 `MG.postfx.frame` 调用并暴露 `MG._quality`。
- **A3 动态背景** `MG.bg`（starfield/aurora/drift）：`?v=off|on`；`_engine` 非分层 clearRect 后调 `MG.bg.draw`（尊重 reducedMotion）。
- **A4 环境天气**：`mg-effects` 的 `fxPool.weather(type,opt)` 持续生成 rain/snow/embers 粒子（trail/flash 此前已有），减弱动效时跳过。
- **A5 调色板皮肤** `MG.skin`（default/neon/sunset/midnight/mono）：`bg(ctx,W,H)` 替代 `MG.gfx.scene` 换肤，`accent()` 取强调色。
- **B6 每日挑战** `MG.daily` + `MG.makeSeed`(FNV-1a 32位)：`seed(d)/today()` 给当天固定种子，配合 `MG.makeRng` 可复现出题。
- **B7 提示解法增强**：`mg-ui` 的 `MG.hint(parent,cfg,api)` 支持 `{text,solver,label}` 并渲染解法按钮（`_engine` 两处 `MG.hint(container,cfg.hint,api)` 传 api）；`main.css` 补 `.mg-hint-text/.mg-hint-btn`（按钮 `pointer-events:auto` 穿透父级 none）。
- **B8 辅助模式** `MG.assist`：`timeScale/extraTime/infiniteUndo`，引擎与游戏读取 `enabled`。
- **B9 分数演出** `MG.scorefx`：`popup/judge(perfect|great|good|miss)/milestone`，复用 `api.pop/api.shake`。
- **B10 本地多人** `MG.localMP(n)`：pass-and-play 回合管理器（current/currentName/next/pass/reset）。
- **C11 崩溃恢复**：`_engine.makeLoop.tick` 把 `advance` 包 try/catch，`tick._errs` 计数，连续 >5 次错误停机并调 `MG.onFatal||MG.showGameError`（避免单帧异常拖死 rAF 循环）。
- **C12 设置云同步**：`MG.settings.sync()` POST `/api/minigame/settings`（端点缺失静默）；`gear()` 面板复用 `MG.i18n.languages` 渲染语言下拉。
- **C13 多语言**：`mg-i18n` 加 `languages:[{zh,中文},{en,English}]`；`gear()` 加语言 `<select>` 调 `setLang`（写 LKEY + `MG.i18n.set`）。
- **C14 PWA 离线**：`MG.pwa.install()` 在 DOMContentLoaded 注册 `/sw.js`，仅 https/localhost/127.0.0.1/152.136.167.250 生效，跳过 `__MG_TEST`；`sw.js` 用 stale-while-revalidate（navigate network-first、静态 cache-first）。
- `mg-settings` 全量重写：新增 `postfx/pixelated/renderScale/dynamicBg/skin/assist/lang` 字段、对应 setter、`_apply()` 同步 `MG.postfx/bg/skin/assist`、`gear()` 面板含 select/checkbox；boot 仍 `load()`。
- 全程用 node 原子补丁脚本（锚点 `indexOf` 校验 + `process.exit(1)` 防误改）规避 EBUSY；临时 `_p_*.js` 跑完即删。
- **硬约束守恒**：`pinball.js`、`pk32*`(18) 未改动；`arcade.js`/`emulator.js` 包装层跳过；`optimized/` 死代码跳过。

## 引擎深度优化（D 性能 / E 体验与网络 / F 工程化，用户"D+E+F 全做"）
新增 10 个引擎模块 + 改 3 个共享文件 + 1 个工具，`?v` 升 `20260918e`，全量 smoke **124/124 通过，零 JS 报错、无空白渲染**（`tools/shots/_smoke_def.txt`）。
- **D1 Worker 卸载** `mg-worker.js`：`MG.worker(fn)` Blob-Worker 离线计算，环境不支持返回 `null`（调用方降级）。
- **D2 对象池** `mg-pool.js`：`MG.pool(factory,reset,cap=512)` 通用池 `acquire/release/each/count/clear`。
- **D3 合图** `mg-atlas.js`：`MG.atlas()` 精灵打包 `add(name,img).build().draw(ctx,name,x,y)`。
- **D4 共享 rAF** `mg-ticker.js`：`MG.ticker` 单 rAF 调度 `add/remove/has/size`，dt 钳 0.1s，纯基建（opt-in）。
- **D5 显存上限** `mg-render.js`：加 `_MAX_BYTES:48MB` + `_bytes(img)` + `_evictMem()`（跨 4 桶按估算字节 LRU 淘汰，guard<256），4 个 insert 位点（px/scene/wood/glow）均调用；原本 `_caches` 已有按数量 LRU（`MAX_CACHE:48`），本项补字节上限。
- **E4 每日挑战 UI** `mg-daily-ui.js`：`MG.dailyUI` 徽章+分享码（`DC-<game>-<seed base36>` `code/parse/mount`）。
- **E5 重放码** `mg-replaycode.js`：`MG.replayCode` 事件 `[{t,x,y,k}]` ↔ `RP-`+btoa / `shareUrl`。
- **E1/E2/E3 网络/云/排行** `mg-net.js`：`MG.net`(WS 脚手架) / `MG.cloud`(localStorage+POST `/api/minigame/cloud`) / `MG.leaderboard`(POST `/api/minigame/leaderboard`)；后端缺失静默 no-op。
- **F1 懒加载模块** `mg-loadmod.js`：`MG._modCache` + `MG.loadModule(url)` 动态注入 `<script>`。
- **F3 性能预算守卫** `mg-perfguard.js`：`MG.perfGuard`（budgetFps:50, enabled:false, set/tick）；`_engine` render 循环接入 `MG.perfGuard.tick(L.perf,quality)`（比 autoQuality 更快降级），`mg-settings` 加 `perfGuard` 开关 + `setPerfGuard` + gear 勾选（用 `data-kind` 排除索引错位）。
- **F4 错误上报** `mg-reporterror.js`：`MG.reportError(err,ctx)` POST `/api/minigame/error`；自动挂 `MG.onFatal = (err)=>{ reportError(err,'fatal'); if(_show)_show(err); }`（崩溃恢复既上报又保留可见错误屏，C11 不回归）。
- **F2 视觉回归工具** `tools/visual-regression.js`：无头 Chrome CDP 逐游戏 8×8 亮度感知哈希；`--baseline` 建 `tools/shots/vreg-baseline/manifest.json`+png，默认跑对比 hamming>8 标"画崩"。
- `main.css` 补 `.mg-daily` 面板样式；`index.html`（`public/`）在 `mg-settings.js` 后注入 10 模块 `<script>`（pool/ticker/worker/atlas/daily-ui/replaycode/net/loadmod/perfguard/reporterror），`mg-render/_engine/mg-settings` 升 `20260918e`。
- 验证：`vm.Script` 解析 14 个引擎文件全 OK；全量 smoke `dtblo5` **124/124 通过**（`_smoke_def.txt`）。
- **硬约束守恒**：`pinball.js`、`pk32*`(18) 未改动；`arcade.js`/`emulator.js` 包装层跳过；`optimized/` 死代码跳过。

## 2P 对战昵称 HUD（用户问"真实多人对战怎么做的 + 游戏界面显示对战双方昵称"）
- 昵称来源：顶栏头像的 `app.user.nickname`（`app.js` 已渲染到头像 chip）。`MinigamesView.open(app)` 时写入 `MG.me={nickname,displayId}`，游戏引擎侧首次接入（此前 MG.* 作用域拿不到）。
- 新增 `MG.match` 会话对象（`mg-net.js`）：`active/mode('local'|'net')/me/opp/side/room` + `begin/setOpp/setSide/setScore/render/end`，驱动 HUD。
- 新增 `MG.matchBar(parent,cfg)`（`mg-ui.js`）：顶栏渲染「你(昵称) VS 对手(昵称)」，回合高亮 + 可选比分，昵称经 `MG.escapeHtml` 防 XSS。
- `mg-net.js`：`join` 带 `me` 昵称；新增 `MG.net.versus(opts)` 联机入口（连 `/ws/minigame` WS 中继，收 `room/peer/side` 消息填 opp）；端点缺失 `connect` 返回 false 静默降级。
- `minigames.js`：hub 加「👥 双人」开关（本地 2P，opp=玩家2）与「🌐 联机」按钮（走 `MG.net.versus`，无后端弹 toast 提示）；`launch` 在 `_versus` 时 `MG.match.begin` 并把 bar 渲染进 `.mini-title`；`close` 时 `MG.match.end`。
- `main.css`：`.mini-vs-actions/.mini-vs-btn/.mg-matchbar/.mg-mb-*` 样式。
- `?v`：main.css/minigames.js/mg-ui.js/mg-net.js 升 `20260918f`（防浏览器缓存旧版）。
- 真实联机架构（待建后端）：原生 `server.js` 已有 `upgrade` 透传 ST socket.io（~4099 行），中继挂同一钩子即可，无需另起进程；休闲/棋牌走"输入中继+同 seed 确定性演算"，动作类走"状态快照中继"，服务端轻量校验。
- 验证：3 文件 `vm.Script` 解析 OK；全量 smoke 首跑 pinball 偶发 `BANKS is not defined`（pinball.js 预存潜在 bug，全仓未定义 BANKS，仅球落袋时触发，flaky），重跑 **124/124 通过 EXIT=0** → 本次改动零回归。
- **pinball.js 仍禁止修改**（BANKS 潜在 bug 非本次引入，按硬约束不动）。

## 联机对战后端 + 入口（user 三问：WS 怎么挂 / 列表单独入口 / 怎么找对手）
- 结论：WS 无需新进程/端口，宝塔 Nginx 已配 Upgrade 透传（deploy/nginx 第 64-65 行），腾讯云安全组只放行 80/443，wss://域名/ws/minigame 直达 Node。中继挂 server.js 同一 upgrade 钩子，与 Tavern 的 ST 透传互不冲突（两者都对非自身路径 return）。
- 匹配方式（user 拍板）：**按游戏隔离的快速匹配 + 房间码邀请（B+C 组合）**，不做 QQ 大厅桌子（A）。即「点开某游戏 → 再匹配」，绝不会五子棋匹配到象棋。
- `server/ws-relay.js`（NEW）：按 game 分桶的 waiting 队列 + 房间配对 + 房间内 input/state 转发 + 30s 心跳清理 + 满员/好友码逻辑；依赖 `ws` 模块（require try/catch，缺失则 no-op 不阻断主服务）。
- `server.js`：顶部 `require('./server/ws-relay')`（失败降级为 null），在 `Tavern.attachUpgrade` 之后 `WsRelay.attach(server)` 仅拦截 `/ws/minigame`。
- `mg-net.js`：`join(room, game)` 与 `versus({game})` 携带 game；`MG._curGame` 由 MinigamesView 写入；versus 新增 waiting/peer_left/error 回调。
- `minigames.js`：分类加「🌐 联网对战」tab（点击卡片走 `startNetVersus(g)`，不加载 50 关）；顶栏「🌐 联机」按钮切到该 tab；`launch` 写 `MG._curGame`；`startNetVersus(g)` 全屏面板含「⚡快速匹配 / 🏠创建房间 / 输入房间码加入」+ 双方昵称 HUD（MG.matchBar 进顶栏）+ 取消。
- `main.css`：补 `.mini-versus-panel/.mvp-*` 面板样式。
- 验证：4 文件 `vm.Script` 解析 OK；全量 smoke **124/124 通过 EXIT=0**（零回归）；`tools/test-ws-relay.js` 端到端 8 项全 PASS（同游戏配对/按游戏隔离/input 转发/建房/房间码加入/peer 更新/满员错误）。
- **双人同步玩法（真正对战内容）尚未实装**——本轮只把匹配/房间/双方昵称 HUD 跑通，下一轮选 1~2 款游戏改造成双人同步。
- `?v`：main.css/minigames.js/mg-net.js 升 `20260918g`。本地运行服务仍是旧代码（未重启），要实测联机需重启 server（宝塔 systemctl restart tower-odyssey，随 push 自动部署）。

## server.js 二次拆分：纯数据常量抽到 server/config/（本会话收尾）
- 用户点名「优秀/精良、震击/增伤、草、落石冲击、水泡泡/溪流精灵」等——全是 server.js 顶部纯数据常量（非逻辑）。
- 抽出到 `server/config/`：qualities.js(7) / heroes.js(9) / walls.js(1) / wish.js(4) / tower.js(5) / buildings.js(5) / ids.js(5) = 36 个 const；`index.js` 用 `Object.assign` 做 barrel 聚合。
- server.js 顶部一次性 `const { ...36个... } = require('./server/config')` 解构引用，删除原声明。
- 行数 3480 → **3348**（减 ~132 行；注释占位吃掉一部分）。所有 36 个 const 在 server.js 内仅被读取，无原地 mutation，抽离安全。
- 注意：`BOSS_SEQ` 依赖 `MAX_FLOOR` 保留在 server.js（依赖逻辑，未抽）；`SMS` 对象方法引用 `crypto`，ids.js 内 `require('crypto')`。
- 验证：`node --check` 全过；测试端口 5199 `/api/health` 200；`require('./server/config')` 断言 17 项（优秀/精良、震击/增伤、草/水/火、落石冲击、水泡泡/溪流精灵、小怪20/Boss8、迷雾森林10章、U_NUM 千分位、SMS 等）全 PASS。一次性脚本 `_extract-config.js`/`_verify-config.js` 跑完即删。
- **剩余 3348 行是真实路由/DB/战斗逻辑**，要再瘦身只能把 `api['METHOD /path']` 路由按域（auth/camp/heroes/tower/clan/admin/minigame/mail…）拆到 `server/routes/*.js`（同 rom.js 的 ctx 注入模式，非数据可删）。待用户确认是否继续。

## 联机匹配卡死修复（用户实测 bug）
- 症状：建房后对方输房间码仍停在「等待对手」，快速匹配也不动。
- 根因：mg-net.js connect() 里 new WebSocket 后立即返回 true，join 在 CONNECTING 阶段同步发出，send 因 readyState!==1 **静默丢弃** → 快速匹配/建房/输码加入三条路全废（服务端从未收到 join）。
- 修复：mg-net.js 加 _pending 队列，onopen 冲刷；新增 onDown 回调，断线时 MG._onVersusError 提示「联机连接已断开」。
- 验证：tools/verify-mg-net-flow.js（真实 ws 客户端 × 真实中继，Node22 全局 WebSocket，vm 双客户端实例）7/7 PASS：建房/凭码加入(side0/1+昵称)/快速匹配自动配对/房间内 input 转发。test-ws-relay.js 8/8 PASS。
- mg-net.js ?v=20260918g→h；需重启线上服务并强刷前端缓存后实测。

## 线上联机 WS 握手挂死【根因 = ws 模块未装，非客户端 bug】
- 现象：线上 ws://152.136.167.250:5180/ws/minigame 升级请求发出后 6 秒零字节响应（raw socket 探活 len=0），浏览器「待处理→超时」。HTTP /api/health 200 正常 → 升级请求到不了 Node 中继。
- 根因：server.js `require('./server/ws-relay')` 包在 try 里，ws-relay 内部 `require('ws')`；**package.json 的 dependencies 此前为空**，线上 `npm install` 从不装 ws → `WsRelay=null` → `if(WsRelay) WsRelay.attach(server)` 跳过 → 无 upgrade 拦截 → socket 挂起。
- 修复：`package.json` 写入 `"ws":"^8.18.0"`（已提交进 HEAD）。本地 `node server.js` 启动打印 `[ws-relay] 联机中继已挂载`，raw 升级探活回 `101 Switching Protocols`（131B）确认接线正确。
- 客户端加固（mg-net.js，?v 20260918g→h，已提交）：connect 加 8s 升级超时 → `_onDown("联机服务无响应…")` 明确报错，不再傻等浏览器超时；_pending 队列 onopen 冲刷（修复 join 在 CONNECTING 被静默丢弃）。
- 验证：tools/verify-mg-net-flow.js 本地完整服务 7/7 PASS；tools/test-ws-relay.js 8/8 PASS。
- **部署关键**：线上只 `git pull`+重启不够——必须 `npm install`（让 ws 进 node_modules）。否则中继仍不挂。重启后浏览器 Ctrl+F5 强刷（吃 ?v=h）。宝塔 `systemctl restart tower-odyssey`，随 push 自动部署时确认流水线含 npm install。

## 联机进房后卡在「50关选关页」修复（用户实测 bug）
- 症状：快速匹配/房间码进房成功，但画面停在 50 关选关页，不进棋盘。
- 根因：`MG.runGame`（mg-ui.js）末尾永远 `showLevels()`；`_launchNet` 把非 banqi 游戏走 `MG.runGame`，所以进房后渲染选关页。且 runGame 自带的 onComplete 走关卡星级结算，`_launchNet` 传的 `onComplete(_pvpResult)` 被忽略。
- 修复：`MG.runGame` 加 `cfg.net` 分支——`net:true` 时跳过 `showLevels()`、直接 `runLevel(0, levels[0])` 进棋盘，且 onComplete 走 `cfg.onComplete`（PvP 结算），不记星级/不上报/不弹关卡结算。`_launchNet` 给 `MG.runGame` 的 cfg 加 `net:true`。
- 验证：tools/verify-runnet.js（极简 DOM 桩加载 mg-ui.js）5/5 PASS：跳过选关/直接 start(levelIdx=0)/不走关卡结算/对局结束走 PvP 结算。
- 缓存：mg-ui.js ?v=20260918f→g，minigames.js ?v=20260918g→h。需重新部署 + 强刷。

## 小游戏列表 UI 重构：卡片双按钮 + 联机筛选 + QQ 游戏大厅（本会话最新）
- 用户诉求：列表每张卡 2 按钮（进入游戏=50关单人 / 联网对战=仅联机游戏，三维弹球无）；加「可联机」筛选项；联机入口改 QQ 大厅式桌子——每座位显示入座昵称，点空位坐下，满座开战。棋类 2 座位，强手棋/大富翁 4 座位。
- `minigames.js`：新增 `NET_GAMES` 注册表（gomoku/banqi/xiangqi/chess/junqi/jungle/ludo/advchess=2 座；monopoly/richman=4 座）+ `NET_WIRED`（前 7 个已真正状态同步对战）。`_renderCard` 渲染双按钮（点卡片=进入游戏，net 按钮=openHall）；`versus` 分类改为仅显示可联机游戏的 2 按钮卡（即筛选）。原 `startNetVersus`（快速匹配/房间码面板）替换为 `openHall`（大厅：桌子列表+创建新桌+座位昵称+满座开战）+ `_renderHallTables/_renderHallMine/_showNetDev`。`MG.net.send('join',{game,cap,create?,room?,me})` 携带昵称；对战复用已有 MG.net 连接（`MG.pvp.commit`→`MG.net.send` 不变）。`?v` minigames.js 升 `20260918h→i`。
- `server/ws-relay.js`：房间加 `cap/started`；新增 `lobby` 订阅 + `tables` 广播（按游戏列未满未开始桌子，空桌不列）+ `seat` 广播（座位占用，长度=cap，空位 null）+ `start`（满座给每人发 side 与对手昵称列表）；join 支持 create/指定房间/自动并入；started 标记防对局中第三者插入；离座即时回收桌子。`?v` 无（纯后端，重启生效）。
- `main.css`：`.mini-acts/.mini-btn(/.play/.net)` 卡片双按钮；`.mg-hall/.mh-*/.mh-seat(/.taken/.me/.empty)` 大厅与座位样式。
- 验证：`tools/test-ws-relay-hall.js`（真实 ws × 真实中继）**23/23 PASS**：2人桌创建/大厅可见/座位昵称/加入/满座start(side0/1)/开战移除桌子；离座回收；4人桌满座start(side0..3)；房间内 input/state 转发。旧的 `tools/test-ws-relay.js` 因 API 改为 seat/start 已废弃删除（覆盖并入 hall 测试）。
- **诚实边界**：monopoly/richman/advchess 大厅与座位已就绪（昵称同步可用），但 4 人真实同步对战**尚未接入游戏模块**（它们的游戏不是 MG.eng net 游戏）→ 满座时 `_showNetDev` 提示「开发中」而非假开战。下一步若要 4 人真对战，需给这些游戏模块接 `cfg.net`（强手棋/大富翁是复杂自定义模块，工作量较大）。2 座位棋类+ludo 已真实打通（前几轮验证）。
- 部署：`git push` → 线上 `git pull && npm install && systemctl restart tower-odyssey` → 浏览器 Ctrl+F5。

## 五子棋联机「各下各的」修复（用户实测 bug，本轮）
- 症状：五子棋 2 人进房后各下各的、互相看不到对方棋子、还能下同一位置。
- 根因：`gomoku.js` 的 `board` 是 `const`，`setState(m){ board = m.board; }` 整体重赋值 `const` → 抛 `TypeError`，被 `MG.pvp._recv` 的 try/catch **静默吞掉** → 对手落子永远进不了本地棋盘。
- 修复：`setState` 改为原地拷贝 `for(i,j) board[i][j] = m.board[i][j]`（board 保持 const）；其余联机逻辑（commit 全量棋盘 + `turn:1-side`、canMove 锁输入、begin 在 side 读取前）本就正确，未动。
- **审计其他联机游戏是否有同类 bug**：只有 gomoku 用了 `const board` 重赋值；xiangqi/banqi 用 `let board = m.board`（重赋值合法）、chess/jungle 用 `S.x=` 原地修改、ludo/monopoly/richman 用 `Object.assign(S,m)`/引擎 `apply` —— 均不抛错，同步模型本身正确，**无其他游戏有此「看不到对方棋子」bug**。
- 验证：给 `tools/verify-pvp-e2e.js` 加 gomoku 回归（加载 `mg-input.js` + `MG.bind` 指针事件驱动落子 + 棋盘收敛/双色断言），`gomoku` 单跑 **8/8 PASS**，全套 ludo 6/6 + gomoku 8/8 通过。
- **已知遗留（非本轮范围）**：monopoly/richman 在 E2E harness 仍 120s 看门狗超时（4 人流程 stall，属独立问题，非「各下各的」同步 bug；可能是 harness 驱动未覆盖全部 phase 或真有 4 人死锁）。用户本轮只报 gomoku，未一并要求修；待用户确认是否继续 4 人局。
- 缓存：gomoku.js 已改，线上需重启 + 强刷（?v 可升 20260918i→j，本次未改 ?v，建议部署时一并升）。

## 联机掉线监测收口（用户问：1人掉线另1人还在棋盘，手机浏览器能监测到退出吗）
- **诊断结论**：掉线**技术上能监测到**——`server/ws-relay.js` 有 `ws.on('close')`（关 Tab/杀进程即时）+ `ws.on('error')` + 30s 心跳（ping/pong，手机锁屏/进电梯/切 WiFi 这种「网络黑洞」最长 ~60s 被发现）→ `cleanup` 给存活玩家发 `peer_left`。但此前**没把 peer_left 收口成「结束对局+弹提示」**：大厅流程（openHall→start→_launchNet）没注册 `MG.net.versus` 的 `peer_left` 钩子；openHall 阶段注册的 peer_left handler 只更新座位 UI（舞台已切棋盘 = 空响）；游戏本身 `MG.pvp.active` 仍是 true、棋盘照旧 → 表现成「对手走了我还在棋盘干等/还能落子」。
- 修复1（服务端 `ws-relay.js`）：心跳 `setInterval` 30s → 15s，手机掉线检测窗口从 ~60s 缩到 ~30s。纯后端，重启生效。
- 修复2（视图层 `minigames.js` `_launchNet`）：对局内统一收口 `MG.net.on('peer_left', ...)` 与 `MG.net.onDown(...)`，二者都 `MG.pvp.end()` + `MinigamesView._pvpResult(g,{win,...},close)`：
  - peer_left（对手掉线）→ 判我方胜（`win:true`，文案 2 人=「对手离开了」、4 人=「有玩家离开了」）+ 弹结果页（再来一局/返回）；并 `MG.net.send('leave',{})` 清理房间。
  - onDown（我方 ws 断开）→ 弹「📡 联机已断开」。
  - close 里加 `MG.net.onDown(null)` 清残留钩子（避免指向上已销毁的 mask）。覆盖所有联机游戏（gomoku/xiangqi/banqi/chess/jungle/ludo/monopoly/richman），不侵入每个游戏文件。
- 验证：harness 加 `verifyDrop`（子命令 `node tools/verify-pvp-e2e.js drop [game]`），模拟 2 种手机掉线——(A) 甲 `ws.close()` 模拟关 Tab/杀进程、(B) 甲 `_socket.destroy()` 模拟断网无 FIN——均验证其余玩家秒级收到 `peer_left` 且恰好 1 次。结果：`gomoku` 2人 5/5 PASS、`richman` 4人 5/5 PASS。
- 部署：`git pull && npm install && systemctl restart tower-odyssey` + 浏览器 Ctrl+F5。改动文件：server/ws-relay.js、public/js/views/minigames.js、tools/verify-pvp-e2e.js（均 ?v 可升 20260918i→j 一并部署）。
- **诚实边界**：手机纯「网络黑洞」（TCP 半开、无 error 无 close）只能靠 15s 心跳兜底，最坏 ~30s 才感知；这是协议层固有限制，已用注释说明，非代码缺陷。
