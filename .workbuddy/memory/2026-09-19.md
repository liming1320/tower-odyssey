# 2026-09-19

## server.js 模块化（续 2026-09-18）
- 抽出第 8 个路由模块 `server/routes/admin.js`：admin/login/mail、account/delete、admin/gift/*、admin/tavern/{config,test,handles,scan}、
  admin/user/{delete,grant}、admin/hero/{add,update,delete,reload}、admin/wall/{update,delete}、admin/event/{save,delete}、
  admin/overview、admin/sms-codes、free、tavern/{ticket,status}。依赖经 routeCtx 注入：
  verifyPassword/normalizeSkills/saveHeroes/Store/Tavern/SMS/romAdminOk/ELEMENTS/ELEMENT_ALIAS。
- **修复既有 bug**：tavern config 路由原调用只在 `rom.js` 作用域内的 `romAdminOk` → 运行时 ReferenceError（后台酒馆配置页 500）。
  在 server.js 顶层补 server 级 `romAdminOk`，经 ctx 注入 admin.js。
- **修复 TDZ**：`normalizeSkills` 的 `const require` 原声明在 routeCtx 之后 → 引用时 `Cannot access before initialization`。
  已上移到 routeCtx 之前。
- **splice 踩坑**：admin 块 splice（admin/mail..主分发）误删了 `require('./server/routes/gift')` 那一行（gift require 夹在 admin/mail 与 tavern/status 之间）→ 路由表缺 gift/redeem。补回该 require。
- **加固 `tools/verify-routes.js`**：造 ADMIN_TOKEN（写 `__admin__`）+ 测试玩家（state + 上阵英雄 h_verify）+ USER_TOKEN，
  实跑 admin/overview/sms-codes/gift/list/tavern/config/minigame/order/pk32/order + tower/level，加「admin 无 token → 403」负向断言。
  结果：**PASS 14 / FAIL 0，路由表 111 个零缺失**（原 6/0）。
- 现状：server.js **~3348 → 1631 行**，8 个 route 模块（camp/heroes/events/tower/clan/minigame/gift/admin），全部零回归。

## 抽出第 9 个路由模块 `server/routes/auth.js`（账号体系）
- 迁出：register/login/sms/send/phone/login、user/bind-phone/set-password/logout/me（8 个）+ 私有 helper `publicUser`。
- 依赖经 routeCtx 注入：hashPassword/verifyPassword/validPhone/maskPhone/genDefaultNickname/defaultUserState/touchLogin/SMS/validNickname/newId/newToken/newDisplayId。
- **关键约束**：`displayName` 仍留在 server.js（被 clan.js 经 ctx 引用），auth 块 splice 时保留 `displayName` 仅移走 publicUser+8 处理器。
- auth.js 顶部自行 `require('crypto')`（原 server.js 第 7 行 require，模块作用域不共享）。
- 闸门再加固：把 `call()` 改为透传 `req.body`，新增实跑 register→login→me（验证 hashPassword/newId/newToken/newDisplayId/defaultUserState/touchLogin/publicUser/verifyPassword/getUserByToken/maskPhone 链路）+「login 错密码→401」负向。
  结果：**PASS 19 / FAIL 0，路由表 111 个零缺失**（原 14/0）。

## 路由模块化收尾结论
- server.js **~3348 → 1471 行**，9 个 route 模块（auth/camp/heroes/events/tower/clan/minigame/gift/admin）。
- 唯一残留内联路由：`GET /api/health`（引导/分发核心，留主文件合理）。
- **`server/config` 扩装备/戒指/宝石/技能纯数据：经核查已非必要**——用户列举的 铁剑/银刃/蓝晶戒/黄玉/藤蔓缠绕 等
  实际都存于 `data/db.json` / `data/heroes.json` 等运行时数据文件；server.js 内只剩生成逻辑用的小名字池（7 项/组，属生成逻辑本身），无需搬。
- 每抽一个域均跑 `node tools/verify-routes.js` 零回归。部署链路此前已自愈（deploy.sh 自动 npm install + ws 校验），纯代码 push 即生效。

## 下一步（可选）
- 纯重构已完成，建议 `git commit` 固化成果（本会话尚未 commit）。如需上线，push 触发 webhook 自动部署。
- 后续若要进一步瘦身 server.js，可把生成逻辑内的小名字池（weapon/ring/artifact/gem 名 + 元素技能模板）抽到 `server/config`，但属优化项、非阻塞。
