# 2026-09-09 · 第三批需求：小游戏奖励 + 暗棋美术大改 + SMS 下线

## 1. 小游戏闯关奖励（服务器端）
- **API**：`POST /api/minigame/report`（game/level/stars）→ 服务器存档 `u.state.minigames[game][level] = { stars, clears }`，防刷奖励
  - 首通：💎 = 10 + 关卡×2 + (星-1)×4，💰 = 400 + 关卡×150
  - 升星补差：💎 = (新-旧)×4，💰 = 100
  - 复通同星 → 0 奖励
- **API**：`GET /api/minigame/progress` → 返回服务器存档
- `_shared.js`：recordStars → 上报服务器 + 失败容错 + 浮层奖励 + 顶栏即时刷新；MG.sync 登录后拉服务器进度合并到 localStorage（换设备不丢进度）
- 奖励浮层 `.mg-reward-toast`（顶部金边卡片 2.7s 自动消失）
- MinigamesView.open 先 MG.sync 再渲染

## 2. 暗棋圣手重写：宋金 Q 版小人 + 击杀必杀技动画
- 从 canvas 完全改写为 DOM/SVG（16 个函数 + 550 行重写）
- **红方（宋国）**：帥仕相俥傌炮兵 → 长翅幞头（两根超长翅超搞笑）+ 红袍
- **黑方（金国）**：將士象車馬砲卒 → 皮草帽 + 小辫 + 蓝黑袍 + 凶眉
- **7 种必杀技动画**（漫画拟声词）：
  - 帅：王之威压（金色冲击波 `威!!`）
  - 仕：拔剑斩（白光剑弧 `唰!`）
  - 相：龟派气功（蓝白光束 + 气功球 `波!!`）
  - 俥：开车碾压（车载小人 + 尘土 + 敌子压扁 `嘀嘀!`）
  - 傌：骑马冲锋（弹跳 + 敌子撞飞 `咴!`）
  - 炮：开炮（炮口火光 + 弹丸飞行 + 爆炸 `轟!`）
  - 兵：红缨枪突刺（星芒戳刺 `戳!`）
- 翻牌 3D rotateY · 被吃棋子托盘 · 道具栏（🔍 透视镜/🪽 羽翼/🍲 大补粥）· 撤销 canvas
- 关键工程：所有动画 setTimeout 链 + 状态机 busy + test 钩子 `B.tap(i,j)`，无头测试与浏览器共用

## 3. 美术美化
- 关卡格子渐变 + 完成态金色高光
- 结算星逐颗弹出（错峰 0/0.14/0.28s）
- hub 卡片：渐变背景 + 圆角缩略图（暗棋专属宋金 VS 小人 SVG）
- CSS 追加 ~200 行于 main.css 末尾

## 4. 短信验证码下线（SMS_ENABLED=false）
- 真实短信需买厂商套餐（约 0.045 元/条），当前未接通
- server.js SMS.send 返回 "短信通道暂未开放，请使用账号密码登录"
- index.html 注掉手机号 tab + 框（保留注释便于恢复）
- login.js / app.js / personal.js 加 null 守卫，前端入口全部摘除
- 后台「验证码」页保留，便于接通后查看历史

## 5. 测试
- `node tools/test-levels.js` 12/12 全过（暗棋规则6 + AI 4 档 100 局 0 异常 + 2048 L1/L5）
- 加固定种子 RNG（setSeed）使随机测试可复现
- `tools/shot-levels.js` 加 `--no-proxy-server` Chrome flag + 持久后台服务器 → 截图通过
- 端到端验证：首通 banqi 第 5 关 2★ → 💎+24/💰+1150 ✓；升星 2→3 → 💎+4/💰+100 ✓；SMS 拒发 ✓

## 6. 提交
- 47b7834 自动包含所有代码改动；截图 + shot-levels 调整增量提交
