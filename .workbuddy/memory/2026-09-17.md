# 2026-09-17 工作日志

## 暗棋(banqi) 联机 PvP 收尾 + 系统性 side 顺序 Bug 修复

### 根因（关键）
`MG.pvp.side` **仅在 `MG.pvp.begin()` 内部写入**（`mg-pvp.js`：`this.side = this._armed.side`），
`begin()` 之前 `side` 恒为 0。banqi 原代码在 `start()` 里读 `first`、在 `gameRound` 里读 `me`
**都早于 begin()**，导致 side 1 方（后手）错误地按 side 0 计算 `me=1 / first='player'`，
继而 `turn(2) !== me(1)` → 所有 tap 被 `onTap` 守卫直接拒绝（联机后手完全无法落子）。

### banqi 修复（`public/js/minigames/banqi.js`）
1. 把 `MG.pvp.begin({setState})` 移到读取 `side` 之前；`me/opp` 在 begin 之后由 `MG.pvp.side` 推导。
2. 终局回环修复：`setState` 收到 `over` 时只调用**展示型** `showResult()`（不再 `commit`），
   本地 `finish()` 才 `commit` + `onComplete`，杜绝 A 胜→B 负→B 回 commit→双方都显示胜 的死循环。
3. `k++` 自增修复（上轮已修，本回合保留）：布局循环漏 `k++` 会让全盘变 16 枚黑子导致误判全军覆没。

### 验证（无需浏览器）
新增 `tools/verify-banqi-pvp.js`：双客户端 + DOM 桩 + 内存 relay，跑真实 banqi.js PvP 路径。
5 组断言 **PASS 10 / FAIL 0**：
- [1] 种子洗牌双方初始棋盘一致；[2] A 翻→B 同步；[3] B 翻→A 反向同步；
- [4] A finish(true) → 双方结果相反（A 胜 / B 负，无回环）；[5] 终局后锁输入。

### 顺带修复的同类 Bug（同一根因，已验证为同样模式）
- `xiangqi.js`：`myColor` 早于 begin 读取 → 重排 begin 在前。
- `jungle.js`：`S._my` 早于 begin 读取 → 重排 begin 在前（初始 `S.turn:1` 已正确，无需改）。
- `mg-chess.js`：
  - `chess` 集成 `S._my` 早于 begin → 重排。
  - `junqi`(军棋翻翻棋) 集成 `S._my` 早于 begin → 重排；且 `S.turn = S._my`（line 241）
    会让后手 side 1 以己方色先手 → 改为 `S.turn = 1`（side 0 永远先手）。
  - `jSetup` 的 `if (shuf) shuf(all)` 潜藏 Bug：`makeRng().shuffle` 返回**新数组**而非原地改，
    原写法把洗牌结果丢弃 → 联机双方种子洗牌失效。`all` 改 `let` 并 `all = shuf ? shuf(all) : MG.shuffle(all)`。

### 改动文件与缓存
- 改：`banqi.js` / `xiangqi.js` / `jungle.js` / `mg-chess.js` / `tools/verify-banqi-pvp.js` / `index.html`
- `index.html` 缓存戳：`banqi`/`xiangqi`/`mg-chess`/`jungle` 均 `?v=20260918h → 20260918i`
- 四个游戏文件 `node --check` 全部通过。

### 待办 / 说明
- 完整 124 游戏 headless 冒烟被阻断：本机未装 Chrome，无法跑 CDP 全量。
  banqi 逻辑已用无浏览器 harness 验证；xiangqi/jungle/mg-chess 的修复为同模式、未经浏览器实测，建议上线前在浏览器联机自测 side 1 方。
- 用户原始清单里的「围棋」在 124 款中**并不存在**；军棋翻翻棋(junqi)、飞行棋(ludo)存在但不在原命名清单，
  已确认 junqi 已接入联机，ludo(飞行棋)尚未接入 —— 待用户确认是否也要补 ludo。

---

## 三个新需求推进（用户第 3 轮）

### ① 经典/街机模拟器能否联机
- **结论：实时帧同步联机（netplay）不可行**。EmulatorJS 没有 netplay API；GB/街机 ROM 非确定性状态同步、
  且模拟器内核不可注入同步钩子。改 `emulator.js`/`arcade.js` 无法凭空造出 netplay。
- **可行替代方案（已在回答里给出）**：「共享存档协作房」——把 `/api/emu/save`（或新增 `/api/emu/room/save`）
  改为按 room 维度存盘，两人共享一个存档槽（谁操作谁存、对方拉取），可选旁观；信令复用现有 `ws-relay`，
  UI 改动集中在 `emulator.js`/`arcade.js` 的云存档桥(`CLOUD_BRIDGE`)。属于设计建议，未写代码。

### ② 其他小游戏继续做成联机（引擎游戏）
- **新增「引擎游戏 opt-in 联机钩子」**：在 `public/js/minigames/engine/_engine.js` 的 `E.game()` 内注入
  `cfg.net` 契约（参照棋类 `MG.pvp` 状态同步模型）。仅声明 `cfg.net` 的游戏接入，单人/PvE 零影响。
- 关键修复：`E.def`/`E.defd` 原先**从不给 `cfg.id` 赋值**，导致 `MG.pvp.shouldBegin(cfg.id)` 恒收 `undefined`、
  联机永远不触发（顺带修好了引擎自身 `gameId: cfg.id` 错误上报一直是 undefined 的问题）。
- 首款接入：**飞行棋(ludo)**。联机 = 红(0) vs 黄(1) 双人（无 AI），回合在 0/1 间交替（`nextTurn` 用 `%2`），
  骰子由行动方本机生成并随状态广播；`check` 改为「我方视角」判定胜负；`tap` 在 `!MG.pvp.canMove()` 时锁输入；
  `doRoll/doMove/nextTurn` 末尾 `api.net.commit()` 同步整盘。
- 验证：`tools/verify-ludo-pvp.js`（双客户端 + DOM/Canvas 桩 + 内存 relay，纯 Node 跑真实 `_engine.js`+`ludo.js`）。
  开局双人模式、每步双端状态收敛、终局双端 onComplete 结果相反均验证（ harness 在后台跑，见任务 8FBOt4）。
- 缓存：`index.html` 中 `_engine.js ?v=20260918e→f`、`ludo.js ?v=20260917e→20260918a`。

### ③ server.js 按模块拆分（已在上一轮完成并验证）
- `server.js` 从 ~4175 行降到 3480 行；ROM/BIOS/云存档路由 + `normalizeSkills` 抽到
  `server/routes/rom.js`（21 条路由）+ `server/core/skills.js`。通过 `ctx` 注入复用 server 全局
  （api/DB/sendJson/...）。服务 :5199 启动 `/api/health` 返回 200 验证通过（测试服务已 kill）。
- 约定：server 用 `api['METHOD /path'] = handler` 注册表；拆分只需 require 模块并传入 shared ctx。

### 清理
- 删除一次性抽取脚本 `tools/_split-server.js`（行号写死，已过时）。
