# 2026-09-17

## 小游戏音效(SFX)补齐 — 持续推进

延续「除 pinball.js 外全部小游戏完善」任务，按安全模式给更多游戏接入引擎级 `MG.audio`：
- 安全模式：每个 `MG.audio.sfx()/unlock()` 都包 `try{…}catch(e){}`；自带 canvas/DOM 的游戏显式 `unlock()`；`E.def` 类走 `MG.canvas` 自动解锁；胜负音由 `MG.result` 统一（避免双音）。
- 本批新加 3 款（动作/休闲/益智）：
  - `alienshoot.js`（孤胆枪手，自绘 canvas）：unlock + 开火→`launch` + 击杀→`target`。
  - `rocketmania.js`（疯狂火箭，自绘 canvas）：unlock + 旋转→`click` + 点火成功→`coin`。
  - `mummymaze.js`（木乃伊迷宫，DOM 网格）：unlock + 移动→`click` + 拾金像→`coin`。
  - `arcade.js` 判定为模拟器-ROM 列表 UI（非可通关小游戏）→ 本轮跳过 SFX。
- `index.html` 三个文件 `?v` 升到 `20260917d`。
- 验证：smoke batch5（alienshoot/rocketmania/mummymaze）后台跑（task N3PsXT）确认 0 JS 报错；batch4（8 款：xiangqi/gomoku/hanoi/bejeweled/banqi/jigsaw/mg-chess/advchess）7/8 通过，`mg-chess` 的 no-game 是误报（注册 id 为 `chess`/`junqi`，非 `mg-chess`）。

## 累计进度
- 引擎级 3 层（juice/result 庆祝音 + 点击涟漪解锁 + MG.canvas 解锁）已覆盖全部 124 款。
- 已有游戏内 SFX 共 39 款（之前 28 + batch5 的 3 + batch6 的 4 + batch7 的 2 + batch8 的 2：mg-sheep/mg-pocketarmy）。
- 本批（休闲/棋牌/动作）：`jungle.js`（斗兽棋，选子+移动/吃子音）、`ludo.js`（飞行棋，掷骰+撞机/归航/移动音）、`mg-knife.js`（鸠摩智转刀，击杀+升级+选技能音）、`richman.js`（强手棋，纯 DOM 需显式 unlock + 掷骰/买地/付租/炒股音）。四个 `?v` 升到 `20260917e`，smoke batch6 后台验证（task toAPL6）。
- 本批续（board/3D）：`monopoly.js`（大富翁，纯 DOM 显式 unlock + 掷骰 click / 买地 coin / 付租 target / 建楼 coin / 出狱卡·罚款 click）、`alienshoot3d.js`（孤胆枪手3D，Three.js 显式 unlock + 开火 launch / 击杀 target）。两个 `?v` 升到 `20260917f`，smoke batch7 后台验证（task uUadWE）2/2 通过。
- 本批续2（mg-* 引擎游戏）：`mg-sheep.js`（羊了个羊，E.defd DOM 需显式 unlock + 选牌 click / 三消 coin / 撤销·打乱 click）、`mg-pocketarmy.js`（口袋奇兵，E.def canvas 引擎自动解锁 + 开火 launch / 击杀 target / 蓝门 coin·红门 click / 木桶 buff coin）。两个 `?v` 升到 `20260917g`，smoke batch8 用正确 id（sheep/pocketarmy）复测（task lm33xF）2/2 通过。
- **EBUSY 坑**：同一文件多 Edit 并发会静默回滚（richman / monopoly / mg-sheep / 连 memory.md 都中招）；固定解法＝同文件多处改动拆成逐条 Edit、逐条 grep 复核，绝不可在同一条消息里对同一文件发两个 Edit（后发者基于旧快照覆盖先发改动）。
- **smoke 误报**：smoke 按注册 id 查游戏，`mg-*` 文件用 `E.def('真实id')`/`E.defd('真实id')` 注册（mg-sheep→sheep、mg-pocketarmy→pocketarmy、mg-knife→knife、mg-chess→chess/junqi），文件名查不到报 `no-game`，需改注册 id 复测。
- **禁止修改 `pinball.js`（三维弹球）**。
- **禁止修改 `pinball.js`（三维弹球）**。
