# PK32 本轮修复与验收记录

日期：2026-09-14。风险等级：中。完整原版迁移：未完成。

## 本轮修复

- 魔塔：接入提取出的开场 4 页、仙子首遇 17 句；原文逐句核对 `strings.json`。
- 魔塔：初始三色钥匙为 0，首遇对话结束后各赋值为 1，仅触发一次。对应原生 RVA `0x1732ce2..0x1732cfe`；英雄初值对应 `0x172b0ba..0x172b1fa`。
- 魔塔：序章不再误标为第一层；原图中铁剑位于第 3 层 `(0,0)`，铁盾位于第 5 层 `(4,3)`，坐标从 0 开始。
- 魔塔：装备从实际拾取记录读取，旧档根据已清除的装备格恢复显示，不再用攻击/防御值猜装备。使用原生剑盾图块。
- 魔塔：对话位于地图内、人物上方，窄屏和尺寸变化时重新定位；逐页确认，对话期间不移动；修复点击操作后键盘焦点丢失。
- 魔塔：新游戏回到序章，存读档保留对话页数、赠物事件、装备和位置。
- 强手棋：地块选择不再误改速度下拉框；移动中重开/退出会取消旧移动定时器。
- 目录：后台资源目录返回不再覆盖正在玩的游戏；返回目录和退出迁移馆统一销毁当前实例。
- 原始 PK32 程序未修改；魔塔 22 张原始地图字符串未改动。

## 已运行验证

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| `tools/verify-pk32-tower-flow.js` | 180 项通过 | 320/390/768/1280 宽度，开场、首遇、对话中途存读档、赠钥匙、进出第一层、开门、首层战斗、重复交互、装备拾取夹具、图块像素、边界与缩放 |
| `tools/verify-pk32-richman-flow.js` | 28 项通过 | 390/1280 宽度，50000 初始现金、单骰子、五步逐格移动、买地、三名 AI、骰子像素位置、速度选择、移动中重开 |
| `tools/verify-pk32-browser.js` | 426 次基础检查通过 | 213 个真实目录入口，各检查手机/桌面一次；另通过两个尺寸下的目录延迟返回和退出销毁检查 |
| `tools/verify-pk32-tower-combat.js` | 299 个战斗用例通过 | 33 个怪物记录、2974 字节原生指令证据；不包含整局流程认证 |
| 地图比对 | 22/22 匹配 | 与已提取原始字符串逐字节比对，不是通关验证 |
| 键盘专项浏览器检查 | 通过 | 开场 Enter、仙子 Enter、点击后方向键继续移动 |
| 静态检查 | 通过 | 修改脚本语法检查、`git diff --check` |
| HTTP 检查 | 通过 | 5180 返回的三个修改脚本与工作区内容一致 |

浏览器报告和截图位于 `output/playwright/pk32-tower-flow/`、`pk32-richman-flow/`、`pk32-browser/`。
浏览器脚本使用 `playwright` 和已安装的 Chrome；本机通过 `NODE_PATH` 使用 Codex 随附的 Playwright，无须改项目依赖。

## 未完成与限制

## 后续进展：电磁彩球

- `public/js/minigames/pk32-variants.js` 已把原先的数字 `<pre>` 展示替换为 16×16 可操作彩球盘面。
- 全部 160 个原生盘面可选；支持方向键/方向按钮、上一关/下一关、撤销；1-4 按数字身份处理为可移动球，5 为固定球。
- `tools/verify-pk32-electromagnetic-flow.js` 已在 390/1280 宽度通过：256 格渲染、160 关、移动、撤销、换关、无横向溢出、无运行时错误。
- 仍保留 `rules-partial`：原始证据没有证明单格移动还是滑行，也没有提供原生计分公式；当前完成判定是原文可证明的“1-4 各自连通”，不是逐像素原版算法认证。
- 原始证据没有证明 1-4 与红/黄/蓝/绿的具体对应关系，因此界面不再虚构颜色名称。

本轮追加修复：电磁彩球改为真实彩球视觉（数字编码仍保留在 `data-value`，颜色仅作显示区分），并完成了 390/1280 宽度的真实浏览器回归。颜色名称和编码对应关系仍不作原版结论。

## 后续进展：爆破彩球

- 将“爆破彩球”从旧的通用泡泡玩法入口移入 PK32 变体专用入口，修复专用渲染器此前无法实际调用的问题。
- 使用 29 条已提取原生盘面；按原文实现上下左右同色连通组，至少 2 个才能消除，消除后按列下落并压缩空列。
- `tools/verify-pk32-burst-flow.js` 已在 390/1280 宽度通过：29 关选择、原生字符、组消除入口、换关、无横向溢出、无运行时错误。
- 仍保留 `rules-partial`：原始数据未给出完整关卡总数和原生计分公式，未宣称逐像素或完整规则等价。

本轮已将“爆破彩球”从 `pk32.js` 的通用 `bubble-match` 入口切换为该专用变体入口；切换后真实浏览器回归仍为 213 款入口无启动错误。

- 不能把入口检查通过当成完整玩法、原版规则或全关卡通关通过。所有报告保留 `fullGameRulesVerified: false`。
- 批量检查发现 7 款含 `pre` 编码文本展示，需继续人工/专用流程核查：同步移动、智慧之光、魔法城堡、魔法城堡二、禅宗迷宫、宇宙黑洞、立体魔方二。两种尺寸合计 14 次待核查。其余游戏也未因基础检查通过就获得规则认证。
- 仙子初遇后的原生侧移/通行时序尚未证实。目前保留原图，先站在相邻格对话，结束后完成进入 NPC 格的原有移动行为；没有编造侧移坐标。此交互是兼容实现，不是原生时序认证。
- 魔塔仍缺完整 NPC/商店状态机、部分机关、战斗逐回合动画和完整结局/通关验证。
- 剑盾拾取测试使用真实地图的局部测试夹具，不代表已经从序章一路通关到剑盾所在楼层。
- 强手棋尚未对全部卡片、破产、存档中断恢复、整局经济规则做原生等价认证。
- 旧 `tools/verify-richman.js` 的 14 项测试验证的是项目原有 50 关强手棋，不是 PK32 强手棋，不能用来证明本次迁移正确。
- 旧 `tools/verify-pk32-visual.js` 仍有过时金额/等待时序断言，本轮不使用它作为验收依据。

## 审查与降级

## 后续纠正：推箱子四

- 前几轮“推箱子四”的目标点规则不正确；新增但未实际接入的猜测函数已删除。
- 从 `module.bin` 的 RVA `0x1bf4a60` 分派表重提取全部 23 关，保留原生关号。旧 19 条对应原版第 22 至第 4 关，顺序相反，还漏了 4 关。
- 原生编码：0 地面、1-4 彩色箱子、5 墙、6 玩家。11×9 居中，外围空白可移动，无目标点。每次仅推一个箱子一格；非单独颜色的每个箱子有同色上下左右邻居即可过关，不要求全连通。
- 接入 PicForm16 箱子、墙、地面与角色；角色采用原生 RGB SRCPAINT。外框使用 PicForm14。没有修改原始 PK32 文件和项目旧小游戏。
- 统一迁移馆“推箱子四”为一个具名启动入口，避免通用 `sokoban` ID 打开第一款示例以及第二个通用益智入口。另修复独立休闲菜单返回/停止时未销毁实例的问题。
- 原生前三关演示共 29 步、600ms 间隔；接入演示、通关确认、胜利冻结动画、23→1 手动循环。重开和销毁取消演示计时器。
- `verify-pk32-sokoban4-data.js`：23 个分支和地图字符串匹配、原始指令守卫通过。
- `verify-pk32-sokoban4.js`：92 个地图方向用例、3 条原生演示、13 处指令字节核对通过。
- `verify-pk32-sokoban4-flow.js`：320/390/768/1280 四种宽度通过，92 次关卡加载、每种宽度 99 格像素比对、3 条原生演示回放、实际目录入口、弹窗、冻结/恢复、撤销、循环、延迟加载和两个入口生命周期验证通过。
- 扩展验证曾遇到快照在返回点击之前采集的计时竞态；已改成返回完成后采集状态，再验证持续不变。保留了前后棋盘和位置不变检查，没有跳过失败断言。
- 213 款 VM 启动检查通过；本轮真实浏览器基础回归 426/426，通过只代表基础检查。另有 12 次视觉待核查，不意味着其余游戏已还原。
- 仍为 `rules-partial`：存档/恢复、原生 UI 状态旁路、完整菜单及 23 关全程通关未认证。旧 `verify-pk32-visual.js` 的 19 图和目标点断言已过时，本次不作为验收依据。
- 两个已有子代理只读核对原生证据；代码审查指出的关卡循环、通关冻结、演示时序和独立菜单生命周期问题已修复并进入浏览器回归。风险中等，未做模型降级。

## 后续证据：智慧之光

- 已反编译确认原版为 140 关，而不是现有 JSON 中的 75 条载荷；其中混入演示和其他游戏记录。
- 已确认 0-59 的类型作用范围、普通状态转换、交换/循环、28/29 后处理、原生美术及 20 条演示。详见 `light-native-analysis.md`。
- 当前统一十字翻灯及全部熄灭初始化不符合原程序，尚待替换，不能计为原版迁移完成。

使用两个现有子代理分别核对原生证据及入口生命周期，并补充代码审查。关于“中途存档丢失对话页”的审查意见经实测证伪，审查方已撤回。未做模型降级；没有用 AI 审查代替真实测试。

## 再次纠正：魔塔开场与短屏验收

- 本节取代前文的“开场 4 页、仙子 17 句”和“让路尚未证实”：原生调用序列实际为开场 5 页、仙子首遇 18 句。补回“而我们的故事，就是从这里开始的……”及“勇士：……”。
- 两句并不在此前的 `strings.json` 中。原清单不是完整性依据；新测试直接按 `module.bin` 中 RVA `0x174bcc4`、`0x1732727` 起始、间隔 `0x2f` 的 push/call 序列读取 BSTR，验证调用目标一致，并与浏览器中实际出现的全部文字按顺序比对。
- 原生 `0x1732a56` / `0x1732a76` 证明首遇后仙子从格 93 移至格 92。运行时根据已保存的首遇标志渲染让路状态，不修改 22 张原始地图。
- 详细流程测试改用 `SettingsView.openPk32()` 的生产全屏容器，点击目录中的魔塔启动按钮；不再直接向临时页面挂载魔塔。未测试账号登录流程。
- 手机改用 320×568、390×844，桌面为 768×960、1280×960。新增的可视区域断言复现了短屏对话中途读档后文字滚出屏幕的问题；修复对话显示、翻页和恢复时滚动至可视区域。
- `verify-pk32-tower-flow.js`：280 项通过，包括生产容器退出销毁、原生完整开场/首遇、让路、短屏逐页可见性、首层开门/战斗和局部装备夹具。截图已人工检查。
- 仙子首遇与无十字架再访后，主角留在相邻格，不自动进入 NPC 格。原生 NPC 调用 `0x1746409` 返回后走清理分支，普通移动位置写入在另一分支 `0x1746681`。无十字架再访两句按 `0x1732f4a` / `0x1732f79` 的 BSTR 核对，并验证双方位置保留。
- `verify-pk32-tower-combat.js`：299 战斗用例、33 怪物、2974 原生指令证据核对通过。213 项 VM 启动检查、脚本语法和 `git diff --check` 通过；启动结果不代表规则认证。
- 5180 的魔塔脚本与工作区逐字匹配，入口缓存版本更新为 `20260914f`。
- 风险中等。仍未完成从序章连续取得剑盾、所有 NPC/商店/机关、逐回合战斗动画及结局验证。所有完成标志继续为 false；没有宣称 213 款迁移完成。
- 只读复核进一步发现：13/14/15 图块误用仙子逻辑，原生分派实际进入 `0x172dd91` 的“神秘商店”；第二层开门标志依赖门内角色，形成循环依赖；第四层铁门没有解锁事件。三层铁剑有模型自然路线，五层铁盾流程存在阻塞，不能用局部拾取夹具宣称完整可达。商店价格、升级数值及具体解锁条件仍需原生证据，不以临时加属性或删门绕过。
